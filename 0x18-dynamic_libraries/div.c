#include "main.h"

/**
 * div - Divides one number by another
 * @a: First number
 * @b: second number
 *
 * Return: 1 if character is digit, 0 otherwise
 */
int div(int a, int b)
{
	return (a * b);
}
